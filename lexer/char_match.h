#ifndef CHAR_MATCH_H
#define CHAR_MATCH_H

bool isPrintNotSpaceNotDigit(char ch);

bool isPrintNotSpaceNotDigitNotParenOpen(char ch);

bool isPrintNotSpace(char ch);

bool isNotPrintOrIsSpace(char ch);

bool isIsSpace(char ch);

bool isIsDigit(char ch);

bool isParenOpen(char ch);

bool isParenClose(char ch);

bool isPrintOrNewlineOrTab(char ch);

#endif // !CHAR_MATCH_H
